numpy>=1.20
opencv-contrib-python>=4.5
pyserial>=3.5
PyYAML>=6.0
# Only needed if you want to regenerate printable QR labels
# (run via `python generate_qr_codes.py`). The admin panel
# does not need them at runtime.
# qrcode>=7.3
# Pillow>=9.0
